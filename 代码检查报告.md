# 代码检查报告

**项目目录**：`/Users/jiangfeng/Desktop/website`
**检查日期**：2026-05-12
**检查范围**：所有 `.js` / `.py` / `.html` / `.css` 文件（不含 `node_modules` 与 `.git`）
**检查重点**：Bug 与逻辑错误、代码规范与可读性

---

## 0. 总览

| 指标 | 结果 |
| --- | --- |
| JS 文件数（不含 node_modules） | 38 |
| HTML 文件数 | 13 |
| Python 文件数 | 2 |
| CSS 文件数 | 10 |
| JS 语法检查 | ✅ 全部通过 |
| Python 语法检查 | ✅ 全部通过 |
| `eval` / `new Function()` | ✅ 0 处（无注入风险） |
| `innerHTML =` 赋值 | ⚠️ 共约 100+ 处，需逐一核对转义 |
| `var` 声明（旧式） | ⚠️ 重灾区：`budget-scorecard-page.js`(136)、`plan-dashboard-render-references.js`(78)、`genbi-page.js`(68) |
| 残留的 `console.log` | `test-genbi-query.js` 12 处 |

**结论摘要**：项目工程化基础良好（模块化、状态层、错误分类都有沉淀），但有 6 个严重问题需立刻修，10+ 个中等问题影响稳定性与可维护性，其余为风格类建议。

---

## 1. 严重问题（建议立即修复）

### S1. `generate_ppt.py:749` — 输出路径硬编码到他人主目录
```python
output_path = "/Users/zhouhao/Desktop/website/广告智投工作台介绍.pptx"
```
当前项目主目录是 `/Users/jiangfeng/...`，写死 `zhouhao` 会在其他机器上 `FileNotFoundError` 或写到错误位置。
**修复**：
```python
import os
OUT_DIR = os.environ.get("PPT_OUT_DIR", os.path.dirname(os.path.abspath(__file__)))
output_path = os.path.join(OUT_DIR, "广告智投工作台介绍.pptx")
```

### S2. `serve.py` — 绑定 `0.0.0.0` 且无优雅退出
```python
server = HTTPServer(("", 8765), handler)
server.serve_forever()
```
- 监听 `""` 等价于 `0.0.0.0`，把本机工作目录全部暴露给同网段；
- 未捕获 `KeyboardInterrupt`，Ctrl+C 不会 `server_close()`，复启时易遇 `Address already in use`；
- `os.chdir(...)` 还会污染调用方进程的全局 cwd。

**修复**：
```python
import os
from http.server import HTTPServer, SimpleHTTPRequestHandler
import functools

ROOT = os.path.dirname(os.path.abspath(__file__))
handler = functools.partial(SimpleHTTPRequestHandler, directory=ROOT)
server = HTTPServer(("127.0.0.1", 8765), handler)
try:
    server.serve_forever()
except KeyboardInterrupt:
    pass
finally:
    server.server_close()
```

### S3. `dashboard-render.js` 多处 `innerHTML` 拼接未转义 `label`
`buildTableRow`（约第 243 行）、`buildCrowdMainRow`（约第 287 行）直接把 `label`（来自后端 `result.ads.monthly[].label`）插入：
```js
let html = `<tr><td>${label}</td>...`;
```
其他行号（`buildCrowdSubRows / buildAudienceRow`）调用了 `escapeHtml`，但本路径漏掉了，造成潜在 **存储型 XSS**——若后端任一环节注入了恶意 HTML 字符串，登录用户的页面会执行任意脚本。
**修复**：所有 label 入参强制 `escapeHtml(label)`，并在仓库引入 ESLint 插件 `eslint-plugin-no-unsanitized` 阻止退化。

### S4. `dashboard.js:326-330` — `setActiveTab` 内引用尚未初始化的顶层 `tabs`
`tabs`/`tabContents` 在 392–393 行才 `const` 声明，但 `setActiveTab` 在更早位置定义并可能被异步路径触发。一旦执行时序变化，会抛 `ReferenceError: Cannot access 'tabs' before initialization`。
**修复**：把 `tabs`/`tabContents` 声明前移；或在函数内自行 `document.querySelectorAll('.tab')` 局部查询。

### S5. `prompt-admin.js:33` — 草稿密钥用 `navigator.userAgent` 派生
```js
// getDraftCryptoKey
hash(window.location.origin + navigator.userAgent)
```
浏览器升级、UA-Reduction、内核切换都会让 UA 变化，**用户重启浏览器后旧草稿会静默丢失**（`decryptDraftContent` 失败直接 `return null`）。同时这并非真正密钥隔离，只是混淆。
**修复**：首次生成一个不可导出的 `CryptoKey` 存到 IndexedDB；密钥派生材料不要包含 UA。

### S6. `test-genbi-query.js:7-8` — Supabase URL 与 SESSION_TOKEN 硬编码
注释为占位但极易被维护者忘记切回提交：
```js
const SUPABASE_URL = '...';
const SESSION_TOKEN = '...';
```
**修复**：改用环境变量并在缺失时立即 `process.exit(1)`：
```js
const SUPABASE_URL = process.env.SUPABASE_URL ?? (() => { throw new Error('SUPABASE_URL is required') })();
```

### S7. `auth/feishu/callback.html:115-128` — Debug payload 写入 localStorage 且不清理
```js
safeSetLocalStorage('feishu_login_last_response', JSON.stringify({...raw_result: result...}));
```
将含 `user_id` / `open_id` / token 的原始响应永久持久化到 localStorage。一旦任何位置出现 XSS，攻击者立即拖走完整登录历史。
**修复**：debug 字段仅在 `?debug=1` 时写；正常流程只保留经过 normalize、最小必要的 user 字段。

### S8. `auth/feishu/callback.html:154-158` — CSRF state 校验被"跨上下文"豁免削弱
```js
const isCrossContext = stateMismatch && !savedState && !!state;
if (stateMismatch && !isCrossContext) { ... }
```
攻击者只要让用户在 `oauth_state` 为空的浏览器中点击精心构造的回调链接，即可绕过校验。
**修复**：保持严格校验，跨上下文场景应引导回 `/auth/index.html` 重新发起授权，而不是直接放行。

### S9. `insights-page.js:401-410` — 隐式全局赋值替换 `renderList`
```js
const _originalRenderList = renderList;
renderList = function(items) { ... };   // 无 var/let/const
```
非严格模式下生成 `window.renderList`，靠"巧合"运作；严格模式立即 `ReferenceError`。任何后续加 `'use strict'` 都会让页面崩溃。
**修复**：
```js
const _originalRenderList = window.renderList;
window.renderList = function(items) { ... };
```

---

## 2. 中等问题（影响稳定性与可维护性）

### M1. `dashboard-export.js:63 / 192` — `URL.createObjectURL` 未 `revokeObjectURL`
两个下载函数缺少释放，多次导出会持续占用内存直到关闭页签（对比第 225 行 `downloadSingleCSV` 正确实现）。
```js
const url = URL.createObjectURL(blob);
link.href = url;
link.click();
setTimeout(() => URL.revokeObjectURL(url), 0);
```

### M2. `dashboard-ai.js:96-146` — AbortController 与超时 timer 错配
`currentAbortController` 是模块级单例，关闭弹窗会被置 null，导致超时 timer 触发 `abort()` 失效；多并发分析时互相干扰。
**修复**：每次请求 `const controller = new AbortController();` 局部持有，并集中维护 `abortHandlers: Set`。

### M3. `dashboard.js:346-368` — `redirectToPromptAdminLogin` 缺 null 校验
直接对 `getElementById(...)` 返回值赋属性；非 dashboard 子页面误触发即抛 `TypeError`。
**修复**：函数首加 `if (!modal || !loading || !body || !text) { window.location.href = '...'; return; }`。

### M4. `prompt-admin.js:223 / 231 / 237 / 247-249` — UI helper 未判空
`showToast`、`setStatus`、`showError`、`setEditorLoading` 全部直接对 `getElementById(...)` 链式赋属性，DOM 一旦变更立即崩溃，反而掩盖真实错误。
**修复**：所有 helper 首行 `if (!el) return;`。

### M5. `dashboard-events.js:114-119` — IIFE 内立即引用 `window.DashboardApp.openAIAnalysis`
```js
const app = window.DashboardApp;
... link.addEventListener('click', () => app.logout());
```
脚本加载顺序变化时 `app` 为 undefined，`addEventListener('click', undefined)` 会抛 `TypeError`。
**修复**：handler 用闭包延迟解引用 `() => window.DashboardApp?.logout()`。

### M6. `script.js:14, 22` — `navMenu.classList.toggle('active')` 缺空校验
外层 `if (hamburger)` 不保证 `navMenu` 存在。
**修复**：合并为 `if (hamburger && navMenu) { ... }`。

### M7. `plan-dashboard-events.js:81` — Excel 序列日忽略 1900-02-29 历史 bug
Excel 把 1900 视为闰年，对 `serial >= 60` 应当减 1，否则早期日期偏移一天。
**修复**：转换前 `if (serial >= 60) serial -= 1;`，或换用成熟库。

### M8. `prompt-admin.js:368` — 前端"黑名单"自命名为 XSS 防御
```js
/<\s*script|javascript\s*:|on\w+\s*=/i
```
此正则可被 `<iframe srcdoc>`、`<img src=x onerror=...>`、CSS expression 等多种载荷绕过。
**修复**：加注释明确"仅前端提示，最终校验在服务端"，并在服务端补 DOMPurify / sanitizer。

### M9. `dashboard-ai.js:138` — 用错误字符串中文匹配做控制流
```js
if (window.__dashboardAuthRedirecting || (error.message || '').includes('AI 分析权限已失效'))
```
未来本地化或后端文案改动会失效。
**修复**：throw 处携带 `error.code = 'AUTH_LOST'`，外层判 `error.code`。

### M10. `index.html` 多处 `innerHTML = ...` 配合 `JSON.stringify(detail)` 作为后备
对象里的特殊字符 stringify 后变 `\"` 等转义符堆栈，用户能直接看到。`renderCrowdTable`、`renderSingleAds` 多处需要把对象 → 文本步骤显式提取字段，而不是 fallback 到 `JSON.stringify`。

### M11. `index.html:867-872` — 手算 "+8 小时" 模拟上海时间
夏令时切换日 / 不同时区下偏移会出错。
**修复**：`Intl.DateTimeFormat('zh-CN', { timeZone: 'Asia/Shanghai', ... }).format(now)`。

### M12. `index.html:60-61` — 内联 `onclick` 与其他页面"严格分离 JS 行为"风格冲突
```html
<button onclick="location.reload()">🔄 刷新数据</button>
<button onclick="window.print()">📄 导出 PDF</button>
```
未来一旦从 CSP 中移除 `'unsafe-inline'`，按钮直接失效。
**修复**：改为绑定 ID + `addEventListener`。

### M13. `metric-rules.html:246-247` — 下拉菜单用 `<span>` 无法聚焦
```html
<li class="nav-item-dropdown">
  <span class="nav-link active">更多 <span class="nav-dropdown-arrow">▾</span></span>
```
其他页面用 `<button type="button" aria-haspopup="true">`。键盘 / 屏幕阅读器无法操作。

### M14. `plan-dashboard-preview.html:77-79` — 三个 nav-item 全部 `href` 相同
```html
<a href="supabase-dashboard.html">🎯 投放分析</a>
<a href="supabase-dashboard.html">👥 人群维度</a>
<a href="supabase-dashboard.html">📦 单品广告</a>
```
没有 fragment、无 tab 切换。
**修复**：改为 `supabase-dashboard.html#ads / #crowd / #single` 并在目标页支持读 hash。

### M15. 退出登录使用 `<a href>`（多个页面）
GET 触发状态变更操作，浏览器预取/链接预览可能误触发，无 CSRF 缓解。
**修复**：换成 `<button type="button" data-action="logout">`。

### M16. `generate_ppt.py` 整文件无 `if __name__ == '__main__':` 守卫
`import generate_ppt` 时立即执行并写文件，无法单元测试或复用。
**修复**：把 SLIDE 1–12 包入 `def build()`，末尾 `if __name__ == "__main__": build()`。

### M17. `dashboard-loader.js:340-376` — `loadAll` catch 中状态文案与 hideLoading 时序错乱
`hideLoading()` 既在 try 中可能被调用，又在 catch 中 `setTimeout(...,1000)`，UI 会闪烁。
**修复**：把 `hideLoading()` 收敛到 `finally`。

### M18. `auth-helpers.js:115-131` — `safeSetStorage` 抛 quota 错误后清理两类 storage
quota 错误仅来自当前目标 storage，但实现里把 `localStorage` 与 `sessionStorage` 同时 prune，会误清缓存。
**修复**：`pruneStorageForQuota(storage)` 仅作用于当前目标。

---

## 3. 轻微问题（风格 / 可维护性）

- **`var` → `let/const`**：`budget-scorecard-page.js`(136 处)、`plan-dashboard-render-references.js`(78)、`genbi-page.js`(68)、`plan-dashboard-render-shared.js`(57) 为重灾区；建议一次性 codemod。
- **`==` → `===`**：`plan-dashboard-render-shared.js`(3)、`plan-dashboard-events.js`(3)、`plan-dashboard-render-references.js`(2)、`plan-dashboard-utils.js`(1)。
- **死代码**：`dashboard.js:21-22` 的 `SB_URL/SB_KEY` 未被使用；`prompt-admin.js:23-25` `latestDraftId` 赋值后从未读取；`insights-page.js:3-4` `SB_URL` 局部变量未使用。
- **魔法数字**：`dashboard-ai.js:97` 超时 `150000` 硬编码；`dashboard-render.js:208-226` ROI/AdShare 阈值（30 / 1）散落各处。建议统一抽 `dashboard-thresholds.js`。
- **重复字段映射**：`dashboard-export.js` 中 `downloadAdsCSV / downloadFullReportCSV` 字段表几乎一模一样。建议抽 `buildAdsCsvRow(row)`。
- **`esc()` 函数（`index.html:254`）未转义单引号**：未来插入到 `'...'` 属性会断属性。建议补 `.replace(/'/g,'&#39;')`。
- **CDN 未锁定子版本**：`index.html:11` 引用 `chart.js@4`，未来不兼容更新会破坏图表。建议改为 `chart.js@4.4.0` 等精确版本 + `integrity` SRI。
- **缺 `type="button"`**：`budget-scorecard.html:62-65`、`plan-dashboard.html:51-55` 等多页未显式声明，移植到 `<form>` 内会变成 submit。
- **可访问性**：`plan-dashboard.html:49` `<div id="month-picker">` 缺 `aria-live`；`genbi.html:120` `<textarea>` 缺 `<label for>`。
- **`generate_ppt.py`**：第 36–67 行 `add_text/add_bullet_list` 未做参数容错；第 148–149 行 `total_w` 可能超过 `W`，导致 `start_x` 为负；第 185 行循环里硬编码尺寸；第 678 行 `icons` 列表每轮重建。
- **`script.js:5`**：`prefers-reduced-motion` 仅初始化时读取，未监听运行时切换。
- **`prompt-admin.js:446-451`**：`input` 事件每次按键都触发 SHA-256+AES-GCM，应 `debounce(300)`。
- **`prompt-admin.js:454-461`**：`beforeunload` 内 async `syncCurrentDraft()`，浏览器不会等待 Promise，存在草稿丢失概率。
- **`test-genbi-query.js`**：12 处 `console.log` 输出 emoji，便于人工查看但不利日志解析；顶层调用未 `await`，进程退出码恒为 0，CI 难判断成败。
- **缩进混合**：`plan-dashboard-events.js:303` 出现 8/4/12 空格混缩，需统一 Prettier 走一遍。
- **大量内联 `style="..."`**：`supabase-dashboard.html:240-242` 单行 11 个内联样式，应迁到 CSS 类。
- **`genbi-rule-admin.html`**：单文件两个 `<style>` 块，可合并。
- **`plan-dashboard-preview.html`**：纯静态 mock，缺 `<meta name="robots" content="noindex">`，可能被搜索引擎抓到。

---

## 4. 按模块的整体评价

### 根目录核心 JS
| 文件 | 评价 |
| --- | --- |
| `auth.js` | 短小职责清晰，仅风格小问题 |
| `dashboard.js` | 模块拆分清晰但顶层变量声明顺序留下时序坑，部分 DOM 操作缺 null 防护 |
| `prompt-admin.js` | 草稿加密思路不错但密钥派生不稳，多处缺 null 校验 |
| `script.js` | 整体清晰，`navMenu` 缺空校验、监听器未解绑是主要瑕疵 |
| `test-genbi-query.js` | 一次性脚本，最大风险是占位凭据被误提交 |

### `assets/js/`（约 28 个模块）
工程化基础相当扎实：每个文件 `(function (window) {})(window)` 包裹，通过挂载 `window.DashboardApi / PlanDashboardState` 等命名空间，在无打包器情况下实现了模块化。状态/请求层（`dashboard-state.js`、`auth-helpers.js`）有版本号、in-flight 去重、quota 重试、错误分类等成熟设计，明显踩过坑迭代过。

主要痛点集中在三块：
1. **渲染层 `innerHTML` 拼接** — 少数路径漏掉 `escapeHtml`，`dashboard-render.js` 是重灾区；
2. **资源生命周期** — 多处 `URL.createObjectURL` 未 revoke、`setTimeout` 与 `AbortController` 错配；
3. **风格不统一** — `var/let/const` 与字符串拼接/模板字面量混用，`insights-page.js` 出现隐式全局赋值这种危险写法。

业务侧（`plan-dashboard-*`、`insights-*`、`genbi-*`、`budget-scorecard`）覆盖了大量复杂交互（导入、抽屉、删除确认、Markdown 渲染），但缺单元测试入口；建议先在 `ai-article-markdown.js`、`auth-helpers.js`、`plan-dashboard-events.js` 的 CSV 解析处补 Jest 测试，再统一引入 ESLint + `no-unsanitized/property` 插件阻止后续 XSS 退化。

### HTML
CSP、`<nav>`/`role` 等语义化在多数页面落实到位，OAuth state 校验与 `innerHTML` 转义工具都做了基础防御，工程化在静态站点里属于中上。但全站缺乏统一的 logout/CSRF 处理；`index.html` 与 `plan-dashboard-preview.html` 混入了大量内联 `onclick`/`style`，与其他页面"严格分离 JS 行为"的风格冲突，是当前最该收敛的债。可访问性细节如 `<span>` 当下拉按钮、`<textarea>` 缺 `<label>`，建议下一轮做专项修。

### Python
`generate_ppt.py` 把整套 PPT 用过程式代码硬写出来，业务逻辑清晰、可读性尚可，但把输出路径硬编码到他人主目录、缺 `__main__` 守卫、缺基础参数校验，是典型"一次性脚本被沉淀到仓库"的通病。`serve.py` 仅 8 行但叠加了三个问题（绑 `0.0.0.0` / 缺 graceful shutdown / `os.chdir` 改全局），在内网中实际等于一个未授权访问入口；建议立即换成 `127.0.0.1` 监听并补 `KeyboardInterrupt` 处理。

---

## 5. 建议的优先级行动清单

1. **本周内修完**：S1、S2、S6、S7、S8（凭据与安全相关），M1、M3、M5（运行时崩溃风险）。
2. **两周内修完**：S3、S4、S5、S9（架构/隐蔽 bug），M2、M4、M11、M14、M15。
3. **长期收敛**：
   - 在 CI 引入 ESLint（`@eslint/js`、`eslint-plugin-no-unsanitized`），把 `innerHTML` 拼接全部纳入告警；
   - 引入 Prettier + EditorConfig，一次 codemod 把 `var` → `let/const`、`==` → `===`、字符串拼接 → 模板字面量；
   - 在 `assets/js/` 的关键工具函数（`escapeHtml`、`safeSetStorage`、CSV 解析、Markdown）补 Jest 单元测试；
   - 为 `serve.py` 切到 `uvicorn` / `python -m http.server` 别名脚本，把开发期与"演示部署"区分清楚。

---

*本报告由 Claude 生成。所有问题均基于实际读取的代码内容，已标注文件名与行号。建议在落地修复前由开发者本人逐条复核，尤其是涉及业务逻辑（如 Excel 1900 闰年、AI 分析超时）的改动。*
