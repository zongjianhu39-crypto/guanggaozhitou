# 预算评分卡改造方案：从 ROI 评分改为人群订单成本评分

> 文档用途：技术开发参考  
> 更新日期：2026-05-02  
> 页面：`budget-scorecard.html`  
> 目标：让评分卡服务日常人群投放复盘和预算调整，而不是按单品或 ROI 做泛化判断。

---

## 一、当前问题判断

### 1. 单品维度不是主要分析视角

当前页面默认展示「单品维度」，并同时请求 `crowd,single` 两类数据。对日常预算分配来说，单品维度容易把分析焦点带到商品表现，而不是人群投放效率。

建议：本页面改为只看「人群维度」，页面命名和文案也围绕“人群预算评分”展开。单品分析如后续需要，可以放到单独页面或作为商品诊断入口，不放在预算评分主流程里。

### 2. ROI 不适合作为本页面核心评分标准

当前公式为：

```text
效率分 = ROI 达标率 × 花费权重 × 100
ROI 达标率 = min(实际 ROI / 目标 ROI, 2.0)
```

业务风险：

- 高客单价、低利润或 0 利润商品会拉高成交额，导致 ROI 看起来不错。
- ROI 只能说明成交额和花费关系，不能说明“买一个订单贵不贵”。
- 做人群预算判断时，更直接的问题是：同样成交一个订单，不同人群分别花了多少钱。

建议：核心指标从 ROI 改为「订单成本」，即：

```text
订单成本 = 花费 / 总成交笔数
```

该字段后端已有基础数据支持：`dashboard-payload.ts` 的 `calcGroup` 已返回 `orderCost`、`orders`、`cost`，前端需要接入这些字段。

### 3. “效率分”业务含义不够清晰

“效率分”听起来像一个黑盒综合分，但页面没有解释它代表什么，也不符合你现在的实际判断方式。

建议：把「效率分」改名为「成本健康分」。含义更清楚：

> 成本健康分 = 该人群订单成本相对目标成本的健康程度，并结合花费规模判断是否值得加减预算。

---

## 二、改造后的页面定位

页面建议命名：

- 主标题：`人群预算评分卡`
- 副标题：`基于订单成本评估人群投放质量，辅助预算加减、暂停和复投判断`
- 导航入口：可继续叫「预算评分卡」或改成「人群预算评分」

页面核心回答 4 个问题：

1. 哪些人群订单成本低，值得加预算？
2. 哪些人群订单成本高，应该减预算或暂停？
3. 当前预算主要花在健康人群还是低效人群？
4. 采纳建议后，7 天后的订单成本是否改善？

---

## 三、功能改造清单

### 1. 移除单品维度

涉及文件：

- `budget-scorecard.html`
- `assets/js/budget-scorecard-page.js`

开发要求：

- 删除或隐藏「单品维度」Tab。
- 默认 `activeTab` 从 `single` 改为 `crowd`。
- 接口请求从 `sections: 'crowd,single'` 改为 `sections: 'crowd'`。
- 删除或停用 `singleItems`、`scoredSingles` 的计算和展示逻辑。
- 状态提示从“共 X 个投放单元”改成“共 X 个人群单元”。

保留建议：

- 如果技术担心后续复用，可以不删后端 single 能力，只在此页面前端不请求、不展示。

### 2. 配置项从 ROI 目标改为订单成本目标

当前配置：

```html
ROI 目标值
```

建议改为：

```html
目标订单成本
```

字段建议：

```javascript
var DEFAULT_CONFIG = {
    orderCostTarget: 80,
    gradeAThreshold: 80,
    gradeBThreshold: 60,
    gradeCThreshold: 40,
};
```

说明：

- `orderCostTarget`：可由业务手动配置，比如 80 元/单。
- 阈值建议初版改成 80/60/40，更符合 0-100 健康分理解。
- 如果不同场次、不同品类目标成本不同，后续可以扩展成按日期/活动/品类保存配置。

### 3. 评分算法改为订单成本健康分

推荐初版公式：

```text
成本达标率 = 目标订单成本 / 实际订单成本
成本健康分 = min(成本达标率, 1.25) / 1.25 × 100
```

解释：

- 实际订单成本越低，分数越高。
- 达到目标成本时，分数约为 80。
- 比目标成本低很多时，最高封顶 100，避免极小订单成本把分数拉爆。
- 实际订单成本越高，分数越低。

示例，目标订单成本为 80 元：

| 人群 | 实际订单成本 | 成本健康分 | 判断 |
|---|---:|---:|---|
| A 人群 | 60 | 100 | 明显优于目标，可加预算 |
| B 人群 | 80 | 80 | 达标，可维持或小幅加预算 |
| C 人群 | 120 | 53 | 偏高，谨慎减预算 |
| D 人群 | 200 | 32 | 明显低效，建议暂停或大幅压缩 |

代码建议：

```javascript
function computeCostHealthScore(orderCost, targetCost) {
    if (orderCost <= 0 || targetCost <= 0) return 0;
    var ratio = targetCost / orderCost;
    return Math.min(ratio, 1.25) / 1.25 * 100;
}
```

是否继续加入花费权重：

- 不建议把花费权重乘进分数里。否则小预算但成本优秀的人群会被低估。
- 建议把花费规模作为单独的“决策条件”，用于控制建议动作。

### 4. 建议动作逻辑不要只看分数

预算动作建议同时看：

- 成本健康分
- 花费规模
- 成交笔数
- 花费占比

建议规则：

| 等级 | 条件 | 动作 |
|---|---|---|
| A 优质 | 分数 >= 80，且成交笔数达到最低样本量 | 加预算 |
| B 健康 | 分数 >= 60 | 维持 |
| C 偏高 | 分数 >= 40 | 减预算 |
| D 低效 | 分数 < 40，或有花费但无成交 | 暂停/压缩 |
| N 样本不足 | 成交笔数低于最低样本量 | 观察，不直接加减 |

建议新增配置：

```javascript
minOrdersForDecision: 5
```

说明：

- 成交笔数太少时，不建议直接判定好坏。
- 例如只成交 1 单且订单成本低，可能只是偶然，不应直接给 A 级加预算。

### 5. 表格字段调整

当前表头：

```text
等级 / 名称 / 花费 / ROI / 效率分 / 花费占比 / 建议动作
```

建议表头：

```text
等级 / 人群 / 花费 / 成交笔数 / 订单成本 / 成本健康分 / 花费占比 / 建议动作
```

可选增加：

```text
成交金额 / ROI
```

但 ROI 只作为辅助参考列，不参与主评分。

### 6. 总览模块文案调整

当前：

```text
效率分布总览
A 超目标 / B 达标 / C 低效 / D 亏损
```

建议：

```text
人群成本健康总览
A 优质 / B 健康 / C 成本偏高 / D 低效
```

预算流向建议保留，但文案改成：

```text
花费流向
A/B/C/D 人群花费占比
```

这个模块对管理判断很有价值：它能直接看出钱是不是花在健康人群上。

### 7. 再分配建议改成“从高成本人群转向低成本人群”

当前逻辑：

- C/D 级削减预算。
- 分配给 A 级。
- 建议卡显示 ROI。

建议改为：

- 从 C/D 人群削减预算。
- 优先分配给 A 级且成交样本足够的人群。
- 建议卡显示订单成本、成交笔数、花费占比。

建议削减比例：

| 等级 | 建议 |
|---|---|
| C 成本偏高 | 削减 20%-30% |
| D 低效 | 削减 50%-70% |
| 有花费无成交 | 暂停或只保留极低测试预算 |

建议分配逻辑：

```text
可转移预算 = C/D 人群建议削减金额之和
接收人群 = A 级 + 成交笔数 >= 最低样本量
按接收人群当前成交笔数或花费占比分配
```

我的建议是初版按「成交笔数占比」分配，比按花费占比分配更贴近日常业务：谁已经稳定出单，谁优先吃预算。

### 8. 执行追踪从 ROI 验证改为订单成本验证

当前追踪逻辑用：

```javascript
effectRoi = item.toRoi - ROI_TARGET
```

建议改成：

```text
执行前订单成本
执行后 7 天订单成本
订单成本变化 = 执行后 - 执行前
```

验证结果：

- 订单成本下降：有效
- 订单成本持平但成交量提升：可接受
- 订单成本上升且成交未提升：无效

追踪卡建议显示：

```text
执行前：订单成本 120 元，成交 20 单
执行后：订单成本 95 元，成交 32 单
变化：订单成本 -20.8%，成交 +60%
```

---

## 四、前后端字段映射

人群接口当前可用字段：

| 前端字段 | 后端来源 | 说明 |
|---|---|---|
| `name` | `group.crowd` / `sub.label` | 人群或子人群名称 |
| `spend` | `summary.cost` / `sub.cost` | 花费 |
| `orders` | `summary.orders` / `sub.orders` | 总成交笔数 |
| `orderCost` | `summary.orderCost` / `sub.orderCost` | 订单成本 |
| `roi` | `summary.roi` / `sub.roi` | ROI，仅辅助展示 |
| `amount` | `summary.amount` / `sub.amount` | 成交金额，可选展示 |

注意：

- 不建议前端用 `spend / conversions` 自己猜字段。
- 现有后端已经叫 `orders` 和 `orderCost`，前端直接使用更稳。

---

## 五、建议技术实施步骤

### 第 1 步：前端视角收敛

- 页面只保留人群维度。
- 默认展示人群数据。
- 请求接口只传 `sections=crowd`。
- 删除或隐藏单品 Tab。

### 第 2 步：指标替换

- `roiTarget` 改为 `orderCostTarget`。
- `ROI_TARGET` 改为 `ORDER_COST_TARGET`。
- `computeEfficiencyScore` 改为 `computeCostHealthScore`。
- 页面所有“效率分”改为“成本健康分”。
- 页面所有主指标 ROI 改为订单成本。

### 第 3 步：数据解析

人群数据解析建议：

```javascript
state.crowdItems.push({
    name: group.crowd,
    spend: toNum(group.summary.cost),
    orders: toNum(group.summary.orders),
    orderCost: toNum(group.summary.orderCost),
    roi: toNum(group.summary.roi),
    amount: toNum(group.summary.amount),
});
```

子人群同理：

```javascript
state.crowdItems.push({
    name: sub.label || sub.name || '未命名人群',
    spend: toNum(sub.cost),
    orders: toNum(sub.orders),
    orderCost: toNum(sub.orderCost),
    roi: toNum(sub.roi),
    amount: toNum(sub.amount),
});
```

### 第 4 步：评分与动作

评分对象建议：

```javascript
return {
    id: 'crowd:' + item.name,
    name: item.name,
    spend: spend,
    orders: orders,
    orderCost: orderCost,
    roi: roi,
    amount: amount,
    spendPct: spend / totalSpend,
    score: score,
    grade: grade,
    action: action,
};
```

### 第 5 步：导出 CSV

导出字段建议：

```text
等级,人群,花费,成交笔数,订单成本,成本健康分,花费占比,ROI,建议动作
```

### 第 6 步：验证

测试用例：

- 有花费、有成交、订单成本低于目标：应为 A 或 B。
- 有花费、有成交、订单成本高于目标很多：应为 C 或 D。
- 有花费、0 成交：应为 D，建议暂停。
- 低成交样本：应标记“样本不足/观察”，不直接建议加预算。
- 导出 CSV 字段和页面字段一致。

---

## 六、建议不要做的事

- 不要继续用 ROI 作为主评分。
- 不要把单品维度作为默认入口。
- 不要把花费权重乘进最终分数，否则会掩盖小预算优质人群。
- 不要只按分数做预算动作，至少要结合成交笔数做样本量保护。
- 不要把“效率分”作为黑盒名称，建议统一叫“成本健康分”。

---

## 七、优先级建议

P0：

- 移除单品 Tab。
- 人群维度默认展示。
- ROI 目标改为目标订单成本。
- 表格主指标改为订单成本。
- 评分公式改为成本健康分。

P1：

- 增加最低成交笔数配置。
- 再分配建议按成交笔数占比分配给优质人群。
- 执行追踪改为订单成本前后对比。

P2：

- 支持不同活动/品类保存不同目标订单成本。
- 加入趋势：近 7 天订单成本变化。
- 加入“花费高但无成交”专项提醒。
